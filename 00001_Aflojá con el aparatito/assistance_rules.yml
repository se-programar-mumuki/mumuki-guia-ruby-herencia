---
rules:
- !ruby/hash:ActiveSupport::HashWithIndifferentAccess
  when: !ruby/hash:ActiveSupport::HashWithIndifferentAccess
    these_expectations_failed:
    - Celular DeclaresComputationWithArity1:utilizar!
  then: a la hora de `utilizar!` un `Celular` es necesario especificar cuántos minutos
    lo haremos
